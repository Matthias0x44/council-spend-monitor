# The portfolio demo needs no environment variables or cloud credentials.
# Optional: set LOCAL_DB_PATH to a different local SQLite file.
# By default the app opens data/demo.db, created by `npm run demo:seed`.
LOCAL_DB_PATH=
